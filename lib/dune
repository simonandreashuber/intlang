(library
 (name intlang_lib)
 (libraries llvm)
 (modules ast errors printIntlang lexer parser include reccheck typecheck monomorph veccheck interp mir printmir buildmir mirgen mirpass calldirectopt tco dceopt compactcfgopt)) ;codegen

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
