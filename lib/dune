(library
 (name intlang_lib)
 (libraries llvm)
 (modules ast errors printIntlang lexer parser include reccheck typecheck monomorph veccheck interp mir printmir mirgen mirpass calldirectopt)) ;codegen

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
