(include_subdirs unqualified)

(library
 (name intlang_lib)
 (libraries llvm)
 (modules ast errors printIntlang 
          lexer parser include reccheck typecheck monomorph veccheck interp 
          mir analysis buildmir printmir
          calldirectopt tco dceopt compactcfgopt memopt
          mirgen mirpipe
          )) ;codegen

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
