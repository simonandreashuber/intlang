(library
 (name intlang_lib)
 (libraries llvm)
 (modules ast lexer parser errors printIntlang include typecheck monomorph interp interp_tast codegen))

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
