include "includetest" -- self include
include "includetest2"
include "inclfoldr/folderincl1" -- relative but in subfolder
let somefunc = \x. x + 1
let main = \() (io.writeln_i32 3) -- included deeply nested, and should be available here
                                  -- still I would find it nice if this was prohibited, ie only use things that are
                                  -- explicitly included...